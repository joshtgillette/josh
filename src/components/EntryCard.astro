---
interface Props {
  href: string;
  title: string;
  description: string;
  date?: Date;
}

const { href, title, description, date } = Astro.props;

const formattedDate = date
  ? date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })
  : null;
---

<a class="entry-card" href={href}>
  <h2 class="entry-card-title">{title}</h2>
  <p class="entry-card-desc">{description}</p>
  {formattedDate && (
    <div class="entry-card-foot">
      <time class="entry-card-date" datetime={date!.toISOString()}>{formattedDate}</time>
    </div>
  )}
</a>
